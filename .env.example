# Module Federation Configuration
# Base URL for hosting federated modules in production
# Used to construct remote entry URLs for module federation
HOST_BASE_URL=https://your-domain.com/

# API Configuration
# Base URL for the API endpoints used across all applications
API_BASE_URL=https://swapi.dev

# Development Configuration
NODE_ENV=development

# CI/CD Configuration
# Set to true when running in CI environment
CI=false
