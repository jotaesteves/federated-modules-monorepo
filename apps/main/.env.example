# Main Application Configuration
# Port for the main application in development
PORT=3000

# Module Federation Host Configuration
# Base URL for remote modules (overrides root .env if needed)
# HOST_BASE_URL=http://localhost:3333/

# Application-specific API configuration (if different from shared)
# API_BASE_URL=https://your-app-specific-api.com

# Build Configuration
# GENERATE_SOURCEMAP=true
# REACT_APP_VERSION=1.0.0
