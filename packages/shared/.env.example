# Shared Package Configuration
# Port for shared package in development (must match module-federation.ts)
PORT=3001

# API Configuration for shared utilities
# API_BASE_URL=https://swapi.dev

# Shared package specific configuration
# REACT_APP_SHARED_CACHE_ENABLED=true
# REACT_APP_SHARED_DEBUG_MODE=false
